import { wx_gotoNewUrl, getUserAddress } from '../../../utils/fn'
const app = getApp()
Page({
  data: {
    addressInfo:{}
  },
  onShow(){
    // getUserAddress().then(res => {
    //   console.log('sss',res)
    // })
  },
})